<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firstdevelop.boot.mapper.StudentMapper">
	<select id="searchALL" resultType="com.firstdevelop.boot.entity.Student">
		select id, name, age, score,address from student
	</select>
	<select id="information" resultType="com.firstdevelop.boot.entity.Student">
		select name, classid,age from student
	</select>
</mapper>